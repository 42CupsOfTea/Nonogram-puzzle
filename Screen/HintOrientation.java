package Screen;

/**
 * Enum for defining the orientation of hint panels in the Nonogram game
 */
public enum HintOrientation {
    // Represents the horizontal hints (for rows) which will be placed at the top of the grid
    ROW,    

    // Represents the vertical hints (for columns) which will be placed on the left of the grid
    COLUMN  
}
